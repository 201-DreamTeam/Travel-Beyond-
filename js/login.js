'use strict';


submit.addEventListener('submit', getdatalog);
function getdatalog(e){
  e.preventDefault();
  
  
  let usernamelog=e.target.username.value;
  let userpasslog=e.target.password.value;
  

 
  
  
}

if (localStorage.user) {

    let login =JSON.parse(localStorage.user);
  for (let i = 0; i < login.length; i++) {
      
     if (login[i] === ) {
         
     } 
      
  }
  }